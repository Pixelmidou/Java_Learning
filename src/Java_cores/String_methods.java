package Java_cores;

public class String_methods {
    public static void main(String[] args) {

        String name = "Spongebob";

        // name.length()
        // name.charAt(1) // what is the char at name[1]
        // name.indexOf("o") // what is the index of the first occurence of 'o' in name
        // name.indexOf("o") // what is the index of the last occurence of 'o' in name
        // name.toUpperCase()
        // name.toLowerCase()
        // name.trim() // removes all right/left white spaces
        // name.replace("o","a") // replaces all 'o' with 'a'
        // name.isEmpty() (true/false)
        // name.contains("x") (true/false)
        // name.equals("bruh") (true/false)
        // name.equalsIgnoreCase("Bruh") (true/false)

        // name.substring(0,3) // parm2 is execlusif
        // name.substring(3) // from 3 to the end

    }
}
